# Deployment

Deploy both sources unchanged to Studionet (`61999`) using the stable py-genlayer dependency header. Deploy the Book and Passbook with their reciprocal addresses using the supported constructor path, then verify source hashes, execution receipts, and canonical reads. Set public addresses only after finalized success. No funded signer is present in this repository; deployment is therefore an external credential blocker rather than a claimed release.
