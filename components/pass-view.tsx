'use client'; import {useEffect,useState} from 'react'; import {readPass} from '@/lib/genlayer/contracts'; export function PassView({id}:{id:string}){const [pass,setPass]=useState<any>(),[error,setError]=useState('');const load=async()=>{try{setPass(await readPass(BigInt(id)))}catch(e:any){setError(e.message)}};useEffect(()=>{void load()},[id]);if(error)return <div className="paper">{error}</div>;if(!pass)return <div className="paper">Loading pass…</div>;const effective=pass.active&&BigInt(pass.expiry)>BigInt(Math.floor(Date.now()/1000));return <div className="paper"><h3>Pass #{pass.id}</h3><p className="decision">{effective?'AUTHORIZED':'EXPIRED / INACTIVE'}</p><p>Stored active: {String(pass.active)} · Evaluation: {pass.evaluation_id}</p><p>Expiry: {new Date(Number(pass.expiry)*1000).toLocaleString()}</p><p>Holder: {pass.holder}</p><p className="hash">{pass.definition_hash}</p></div>}
