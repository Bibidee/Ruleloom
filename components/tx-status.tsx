'use client'; import {TxState} from '@/lib/genlayer/transaction'; import {explorerTx} from '@/lib/genlayer/network'; export function TxStatus({state}:{state:TxState}){if(state.phase==='IDLE')return null;return <aside className="tx"><b>{state.phase.replaceAll('_',' ')}</b>{state.hash&&<a href={explorerTx(state.hash)} target="_blank">View transaction ↗</a>}{state.error&&<small>{state.error}</small>}</aside>}
