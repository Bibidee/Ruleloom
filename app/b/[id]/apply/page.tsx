import {ApplicationForm} from '@/components/application-form'; export default async function Apply({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main><span className="eyebrow">Application / frozen evidence</span><h2>Make your case.</h2><p>Public sources are locked into your application before independent clause-by-clause evaluation.</p><ApplicationForm bookId={id}/></main>}
