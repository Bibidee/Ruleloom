import {Receipt} from '@/components/receipt'; export default async function AppReceipt({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main><span className="eyebrow">Evaluation receipt / canonical readback</span><h2>Findings become<br/>a decision.</h2><p>The evaluator may interpret evidence, but it never chooses a pass duration or arbitrary action.</p><Receipt id={id}/></main>}
